export enum ErrorTypes {
  ERROR_MORE_CHARS_THAN_ALLOWED = 'Has more chars than allowed',
  DEADLINE_BEFORE_TODAY = 'Cannot set a deadline date before than today',
  NO_TODO_TO_DELETE = "Todo couldn't be found to be deleted",
}
